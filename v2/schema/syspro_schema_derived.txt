# SYSPRO Schema (Derived)
# Source of truth: columns extracted from 2,988 verified SQL templates in
# app/query_engine/templates/all_templates_combined.json
# Every column below appears in at least one working T-SQL template.
# Generated: 2026-07-02

TABLE ApInvoice  (AP Invoice — Accounts Payable invoices)
  Branch, Currency, CurrencyValue, DiscActiveFlag, DiscountDate, DueDate,
  FirstTaxCode, Invoice, InvoiceDate, InvoiceMonth, InvoiceStatus,
  InvoiceYear, MthInvBal1, OrigDiscValue, OrigInvValue, PaymDiscValue,
  Reference, RevChargeFlag, Supplier

TABLE ApSupplier  (AP Supplier master)
  Currency, CurrentBalance, LastChequeDate, LastChequeVal, LastPurchDate,
  PurchValMth1..PurchValMth12, PyrFreightVal, PyrMerchVal, PyrOtherVal,
  Supplier, SupplierClass, SupplierName, WithTaxCode, WithTaxId

TABLE ArInvoice  (AR Invoice — Accounts Receivable invoices)
  Branch, Currency, CurrencyValue, Customer, DocumentType, DrCrInvoiceValue,
  DrCrTaxDiscPortion, DrCrTaxPortion, DueDate, Invoice, InvoiceBal1,
  InvoiceDate, MthInvBal1, OrigDiscValue, OrigInvValue, OrigTaxOnDisc,
  OrigTaxableAmt, OrigTaxableDisc, PostCurrency, SalesOrder, Salesperson,
  Supplier, TaxCode, TaxPortion, TermsCode

TABLE ArCustomer  (AR Customer master)
  Area, Branch, CreditLimit, CreditStatus, Currency, Customer,
  CustomerOnHold, DateCustAdded, DateLastPay, DateLastSale, IbtCustomer,
  MasterAccount, Name, Nationality, OutstOrdVal, PaymentStatus,
  Salesperson, State

TABLE ArCustomerBal  (AR Customer balances / ageing buckets)
  CurrentBalance, CurrentBalance1, CurrentBalance2, CurrentBalance3,
  Customer, Val120daysInv, Val30daysInv, Val60daysInv, Val90daysInv,
  ValCurrentInv

# NOT PRESENT in the template corpus (do not map without a real schema):
#   PorMasterHdr / PorMasterDetail (Purchase Orders) — no PO tables are
#   referenced by any template. The purchase_order adapter is therefore
#   intentionally omitted until a PO schema is provided.
